Let me tell you a little bit about MarketGold. His journey started when he was in University, where he was introduced to Forex. So at that time, he was trading forex but couldn't get clarity. So fast forward after completing University, he was stuck in a room always looking at the chart, trying to find his way on the chart. At that moment, he sometimes collect money from his mother to trade which he always loses and there was pressure from the family that what are you doing inside, why are you always on your laptop, is this thing going to work and those kind of stuffs. But fast forward, he didn't give up and was always trying. And now, things has worked out and now he sometimes do tours to educate youngsters about forex trading. He has a partnership with SNS and many other things. So now he has an academy that he teaches other people. So that is kind of some brief of his life span on this earth.



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


 whileHover={{ scale: 1.05, boxShadow: "0 0 25px rgba(212,175,55,0.5)" }}
          whileTap={{ scale: 0.95 }}

telegram channel:https://t.me/marketgodcommunity - 13k members

facebok : https://web.facebook.com/eyram.akpey
x:   https://x.com/eyramdela   -5.2k followers
youtube  https://www.youtube.com/@marketgodcommunity  -  21.2k subscribers
ig : https://www.instagram.com/eyram_dela   -33.7k followers
personal telegram : https://t.me/delatrades
tictok : https://www.tiktok.com/@marketgodcommunity - 10K 
personal- ticok  - https://www.tiktok.com/@eyramdela_?_r=1&_t=ZM-9229kTUvSTT
exness link : https://one.exnessonelink.com/boarding/sign-up/303589/a/eyram
og:https://res.cloudinary.com/dzqdfaghg/image/upload/v1763578745/Black_White_Gradient_Digital_Marketing_Instagram_Post_f2zh1x.png

bot: https://t.me/livetradewithmarketgodbot
bot: https://t.me/@paymarketgodbot
payment:VIP SIGNALS : @paymarketgodbot
admin @delatrades 
YouTube : https://youtube.com/@marketgodcommunity?si=JjxdzKvyr3fYc5EL




  const socials = [
    { name: "Telegram", icon: Send, link: "https://t.me/marketgodcommunity" },
    { name: "Facebook", icon: Globe, link: "https://web.facebook.com/eyram.akpey" },
    { name: "Instagram", icon: Globe, link: "https://www.instagram.com/eyram_dela" },
    { name: "Youtube", icon: Youtube, link: "https://www.youtube.com/@marketgodcommunity" },
  ];







// src/components/Webinars/Webinars.tsx
import  { useState } from "react";
import { motion, AnimatePresence } from "framer-motion";
import { useTheme } from "../../context/ThemeContext";
import { Calendar, Video, MapPin, X, ArrowRight, Send, Globe, Zap, Youtube } from "lucide-react";

const Webinars = () => {
  const { theme } = useTheme();
  const [showPopup, setShowPopup] = useState(false);

  const events = [
    {
      type: "Free Weekly Webinar",
      title: "Price Action Mastery",
      date: "Every Thursday, 7 PM GMT",
      desc: "Live breakdown of market setups, Q&A, and Sniper Strategy demo. Perfect for Ghana evenings.",
      topics: ["Price action entries", "Risk management", "Live trade analysis"],
      price: "Free",
      join: "Register via Telegram",
      href: "https://t.me/delatrades",
      icon: Video,
    },
    {
      type: "Sniper Seminar",
      title: "Sniper MarketGod Strategy Workshop",
      date: "Dec 15, 2025 – 9 AM GMT (Virtual)",
      desc: "Half-day interactive session with live trading simulations and 1-on-1 feedback. Limited to 100 spots.",
      topics: ["Building trading systems", "Psychology mastery", "Ghana case studies"],
      price: "$67 (Early Bird)",
      join: "Book on Telegram",
      href: "#",
      icon: Calendar,
       triggerPopup: true,
    },
    {
      type: "Free Physical Seminar",
      title: "Accra Live Trading Day",
      date: "Occasional In-Person Events (Accra)",
      desc: "We host powerful in-person trading days in Accra — live trading, networking, and free lunch.",
      topics: ["Live market execution", "Trader meetups", "Strategy reviews"],
      price: "Free",
      join: "Get Notified",
      href: "#",
      icon: MapPin,
      triggerPopup: true,
    },
  ];

  const socials = [
    { name: "Telegram", icon: Send, link: "https://t.me/marketgodcommunity" },
    { name: "Facebook", icon: Globe, link: "https://web.facebook.com/eyram.akpey" },
    { name: "Instagram", icon: Globe, link: "https://www.instagram.com/eyram_dela" },
    { name: "Youtube", icon: Youtube, link: "https://www.youtube.com/@marketgodcommunity" },
  ];

  return (
    <>
      {/* 🌅 Background Section */}
      <section
        id="webinars"
        className="relative py-24 overflow-hidden"
      >
        {/* 🖼️ Background Image */}
        <div
          className="absolute inset-0 bg-cover bg-center"
          style={{
            backgroundImage:
              "url('https://res.cloudinary.com/dzqdfaghg/image/upload/v1763522353/SnapInsta.to_344115183_2414894028679317_1325419865450439379_n_uqflwo.jpg')", // Add your image in /public/images/webinars-bg.jpg
          }}
        />

        {/* 🔮 Gradient Overlay */}
        <div className="absolute inset-0 bg-gradient-to-br from-black/80 via-black/60 to-transparent" />

        {/* 🌍 Animated Glow Overlay */}
        <div className="absolute inset-0 bg-[radial-gradient(circle_at_20%_30%,rgba(255,215,0,0.08),transparent_70%),radial-gradient(circle_at_80%_70%,rgba(34,197,94,0.08),transparent_70%)] animate-pulse-slow" />

        <div className="relative z-10 max-w-7xl mx-auto px-6">
          {/* Header */}
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            className="text-center mb-16"
          >
            <h2 className="text-4xl md:text-6xl font-black tracking-tight mb-4 text-yellow-400 drop-shadow-lg">
              Webinars & <span className="text-yellow-600">Seminars</span>
            </h2>
            <p className="text-xl md:text-2xl max-w-3xl mx-auto text-gray-300">
              Live sessions with Eyram Dela. Hands-on trading. Ghana time.  
              <span className="text-yellow-400 font-semibold"> No AI — just mastery.</span>
            </p>
          </motion.div>

          {/* Events Grid */}
          <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
            {events.map((event, i) => (
              <motion.div
                key={i}
                initial={{ opacity: 0, y: 40 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true }}
                transition={{ delay: i * 0.1 }}
                whileHover={{ y: -8 }}
                className={`relative p-8 rounded-3xl border backdrop-blur-md transition-all ${
                  theme === "light"
                    ? "bg-white/90 border-yellow-400/30 shadow-xl"
                    : "bg-black/70 border-yellow-500/40 shadow-2xl"
                }`}
              >
                <div className="flex items-center gap-3 mb-4">
                  <event.icon size={26} className="text-mg-gold" />
                  <span className="text-sm font-bold uppercase text-mg-gold">
                    {event.type}
                  </span>
                </div>

                <h3 className="text-2xl font-bold mb-3 text-yellow-500">
                  {event.title}
                </h3>
                <p className={`text-base mb-2 text-black   ${
                  theme === "light"
                    ? "text-black/90 border-yellow-400/30 "
                    : "text-white/70 "}`}>{event.date}</p>
                <p className={`text-base mb-4 ${
                  theme === "light"
                    ? "text-black/90 border-yellow-400/30 "
                    : "text-white/70 "}`}>{event.desc}</p>

                <div className="space-y-2 mb-6">
                  <p className="text-sm uppercase tracking-wider text-yellow-500">Topics:</p>
                  <ul className={`text-sm ${
                  theme === "light"
                    ? "text-black/90 border-yellow-400/30 "
                    : "text-white/70 "} space-y-1`}>
                    {event.topics.map((topic, j) => (
                      <li key={j} className="flex items-center gap-2">
                        <Zap size={12} className="text-yellow-400" />
                        {topic}
                      </li>
                    ))}
                  </ul>
                </div>

                <div className="mb-4">
                  <div className="text-xl font-bold text-yellow-500">{event.price}</div>
                  <p className={`text-sm ${
                  theme === "light"
                    ? "text-black/90 border-yellow-400/30 shadow-xl"
                    : "text-white/70"}`}>{event.join}</p>
                </div>

                    {eventtriggerPopup ?  motion.button
             
                  onClick={() => event.triggerPopup && setShowPopup(true)}
                  whileHover={{ scale: 1.05 }}
                  className={`inline-flex items-center gap-2 px-6 py-3 rounded-full font-bold uppercase tracking-wide transition-all w-full justify-center ${
                    event.price === "Free"
                      ? "bg-yellow-500 text-black"
                      : "border-2 border-yellow-500 text-yellow-400 hover:bg-yellow-500/10"
                  }`}
                >
                  {event.price === "Free" ? "Register Free" : "Book Now"}
                  <ArrowRight size={18} />
                </motion.button> : <a href={event.href}  className={`inline-flex items-center gap-2 px-6 py-3 rounded-full font-bold uppercase tracking-wide transition-all w-full justify-center bg-yellow-500 text-black"
                     
                  }`}>Free</a> }
                

                >

                <motion.button
             
                  onClick={() => event.triggerPopup && setShowPopup(true)}
                  whileHover={{ scale: 1.05 }}
                  className={`inline-flex items-center gap-2 px-6 py-3 rounded-full font-bold uppercase tracking-wide transition-all w-full justify-center ${
                    event.price === "Free"
                      ? "bg-yellow-500 text-black"
                      : "border-2 border-yellow-500 text-yellow-400 hover:bg-yellow-500/10"
                  }`}
                >
                  {event.price === "Free" ? "Register Free" : "Book Now"}
                  <ArrowRight size={18} />
                </motion.button>
              </motion.div>
            ))}
          </div>

          {/* CTA */}
          <motion.div
            initial={{ opacity: 0 }}
            whileInView={{ opacity: 1 }}
            viewport={{ once: true }}
            transition={{ delay: 0.5 }}
            className="text-center mt-16"
          >
            <motion.a
            target="_blank"
              href="https://www.instagram.com/eyram_dela"
              whileHover={{ scale: 1.05 }}
              whileTap={{ scale: 0.95 }}
              className="inline-flex items-center gap-3 px-10 py-5 bg-yellow-500 text-black rounded-full font-bold uppercase tracking-wider shadow-xl"
            >
              Stay Updated on Events
              <Calendar size={24} />
            </motion.a>
          </motion.div>
        </div>
      </section>

      {/* Popup */}
      <AnimatePresence>
        {showPopup && (
          <motion.div
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            exit={{ opacity: 0 }}
            className="fixed inset-0 bg-black/60 backdrop-blur-sm z-[999] flex items-center justify-center p-4"
            onClick={() => setShowPopup(false)}
          >
            <motion.div
              initial={{ scale: 0.8, y: 50 }}
              animate={{ scale: 1, y: 0 }}
              exit={{ scale: 0.8, y: 50 }}
              transition={{ type: "spring", damping: 25 }}
              className="relative max-w-lg w-full p-8 rounded-3xl shadow-2xl bg-black/80 border-2 border-yellow-500/40"
              onClick={(e) => e.stopPropagation()}
            >
              <button
              aria-label="Close popup"
                onClick={() => setShowPopup(false)}
                className="absolute top-4 right-4 p-2 rounded-full bg-white/20 hover:bg-white/30 transition"
              >
                <X size={20} className="text-yellow-400" />
              </button>

              <div className="flex justify-center mb-6">
                <div className="p-4 bg-yellow-400/10 rounded-full">
                  <Calendar size={48} className="text-yellow-400" />
                </div>
              </div>

              <h3 className="text-2xl font-black text-center mb-4 text-yellow-300">
                No Events Right Now
              </h3>

              <p className="text-center mb-8 leading-relaxed text-gray-400">
                We currently don’t have upcoming physical seminars.  
                Follow us to be notified when the next one drops in{" "}
                <span className="text-yellow-400 font-bold">Accra</span>.
              </p>

              <div className="space-y-3">
                <p className="text-center font-bold uppercase tracking-wider text-yellow-400 mb-4">
                  Stay Updated — Join Us:
                </p>
                {socials.map((social, i) => (
                  <motion.a
                    key={i}
                    href={social.link}
                    target="_blank"
                    rel="noopener noreferrer"
                    whileHover={{ scale: 1.03 }}
                    className="flex items-center justify-center gap-3 p-4 rounded-xl border border-yellow-500/30 bg-black/60 hover:bg-yellow-500/10 transition-all"
                  >
                    <social.icon size={24} className="text-yellow-400" />
                    <span className="font-bold text-yellow-300">{social.name}</span>
                    <ArrowRight size={18} className="text-yellow-400" />
                  </motion.a>
                ))}
              </div>
            </motion.div>
          </motion.div>
        )}
      </AnimatePresence>
    </>
  );
};

export default Webinars;


I RECEIVE THIS MESSAGE FROM MY CLIENT CONCERNING HIS reviews ON THE SITE I CREATED hello good evrning So, he sent me the link of your website, the website you created for him, and he wants you to do a little changes. His colors are black and white, the marker code looks mostly black and white. So, he wants you to change the red, green, like the colors. But then I'm still looking for it, so I'll jot down the things that you need to change and I'll let you know. Yes, but this is just by the way, I just wanted to let you know that this is what he said. So yeah, I'll get back to you. Thank you.  

SO I DECIDED TO DO THIS


MarketGod Color Customization Guideline
1. Overall Theme

Primary Colors: Black (#000000) and White (#FFFFFF)

Purpose: Keep a clean, modern, premium look. Use high contrast for readability and simplicity.

2. Headlines / Text

Main Headings (H1, H2, H3): Black (#000000)

Body Text: Dark Grey (#1A1A1A) for readability

Keywords: Optional accent color (one per headline if needed)

Examples: Gold (#C9A227) or Blue (#1E90FF)

Rules:

Avoid using multiple colors in a single headline

Only highlight key words in accent color

3. Backgrounds

Primary Background: White (#FFFFFF)

Section Backgrounds / Cards: Light Grey (#F5F5F5) or Medium Grey (#E6E6E6)

Call-to-Action Sections / Hero Sections: Black (#000000) with White text

4. Buttons

Primary Buttons: Black background (#000000) + White text (#FFFFFF)

Secondary Buttons: White background (#FFFFFF) + Black border + Black text (#000000)

Hover States: Invert colors or use subtle grey accent

5. Icons / Borders / Lines

Stick to Black (#000000) or Grey (#808080)

Keep it consistent across all pages

6. Links / Interactive Text

Use Black (#000000) by default

Accent color only for key actions or keywords

Avoid multiple colors on the same line

7. Popups / Modals

Background: White (#FFFFFF)

Text: Dark Grey (#1A1A1A)

Buttons: Follow button rules above

8. Key Considerations

Maintain high contrast for readability

Keep accent colors minimal — only for emphasis

Headlines should generally be single color, except key words

Consistency is crucial across pages                so i changed this file from // tailwind.config.js
module.exports = {
  darkMode: "class",
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {
      colors: {
        mg: {
          gold: "#D4AF37",
          green: "#00c896",
          paper: "#F2F2F2",
          charcoal: "#111111",
          black: "#000000",

          light: {
            bg: "#FAFAFA",
            surface: "#FFFFFF",
            border: "#E5E7EB",
            text: "#1F2937",
            textSecondary: "#6B7280",
          },
          dark: {
            bg: "#000000",
            surface: "#111111",
            border: "#1F2937",
            text: "#F2F2F2",
            textSecondary: "#9CA3AF",
          },
        },
      },
      boxShadow: {
        "gold-glow": "0 0 20px rgba(212, 175, 55, 0.5)",
        "gold-glow-light": "0 0 15px rgba(212, 175, 55, 0.3)",
      },
      animation: {
        lightStreak1: "lightStreak 4s linear infinite",
        lightStreak2: "lightStreak 6s linear infinite reverse",
        lightStreak3: "lightStreak 5s linear infinite",
      },
      keyframes: {
        lightStreak: {
          "0%": { transform: "translateX(-100%)" },
          "100%": { transform: "translateX(100%)" },
        },
      },
    },
  },
  plugins: [],
};                        @tailwind base;
@tailwind components;
@tailwind utilities;

@import url("https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700;800;900&display=swap");

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  font-family: "Montserrat", sans-serif;
  transition: background-color 0.3s ease, color 0.3s ease;
  background-color: #FAFAFA; /* light default */
  color: #1F2937;
}

.dark {
  background-color: #000000;
  color: #F2F2F2;
}

/* Scrollbar */
::-webkit-scrollbar {
  width: 8px;
}
::-webkit-scrollbar-track {
  @apply bg-mg-light-border dark:bg-mg-dark-border;
}
::-webkit-scrollbar-thumb {
  @apply bg-mg-gold;
}
::-webkit-scrollbar-thumb:hover {
  @apply bg-yellow-500;
}

/* Light Streak Animation */
@keyframes lightStreak {
  0% { transform: translateX(-100%); }
  100% { transform: translateX(100%); }
}

/* Hide scrollbar for Chrome, Safari, Opera */
.no-scrollbar::-webkit-scrollbar {
  display: none;
}

/* Hide scrollbar for Firefox */
.no-scrollbar {
  scrollbar-width: none;
}

/* Hide scrollbar for IE & Edge */
.no-scrollbar {
  -ms-overflow-style: none;
}
.no-scrollbar::-ms-scrollbar {
  display: none;
}               to     module.exports = {
  darkMode: "class",
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {
      colors: {
        mg: {
          gold: "#D4AF37",
          black: "#000000",
          paper: "#FFFFFF",

          light: {
            bg: "#FAFAFA",
            surface: "#FFFFFF",
            border: "#E5E7EB",
            text: "#1F2937",
            textSecondary: "#6B7280",
          },
          dark: {
            bg: "#000000",
            surface: "#111111",
            border: "#1F2937",
            text: "#F2F2F2",
            textSecondary: "#9CA3AF",
          },
        },
      },
      boxShadow: {
        "gold-glow": "0 0 20px rgba(212, 175, 55, 0.5)",
        "gold-glow-light": "0 0 15px rgba(212, 175, 55, 0.3)",
      },
      animation: {
        lightStreak1: "lightStreak 4s linear infinite",
        lightStreak2: "lightStreak 6s linear infinite reverse",
        lightStreak3: "lightStreak 5s linear infinite",
      },
      keyframes: {
        lightStreak: {
          "0%": { transform: "translateX(-100%)" },
          "100%": { transform: "translateX(100%)" },
        },
      },
    },
  },
  plugins: [],
};

};@tailwind base;
@tailwind components;
@tailwind utilities;

@import url("https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700;800;900&display=swap");

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  font-family: "Montserrat", sans-serif;
  @apply bg-mg-light-bg text-mg-light-text transition-colors duration-300;
}

/* Dark mode */
.dark body {
  @apply bg-mg-dark-bg text-mg-dark-text;
}

/* Headline accent */
.text-accent {
  @apply text-mg-gold;
}

/* Buttons */
.btn-primary {
  @apply bg-mg-black text-mg-paper px-6 py-2 rounded hover:shadow-gold-glow transition;
}

.btn-secondary {
  @apply bg-mg-paper text-mg-black border border-mg-black px-6 py-2 rounded hover:shadow-lg transition;
}

/* Scrollbar */
::-webkit-scrollbar {
  width: 8px;
}
::-webkit-scrollbar-track {
  @apply bg-mg-light-border dark:bg-mg-dark-border;
}
::-webkit-scrollbar-thumb {
  @apply bg-mg-gold;
}
::-webkit-scrollbar-thumb:hover {
  @apply bg-yellow-500;
}

/* Hide scrollbar for Chrome, Safari, Opera */
.no-scrollbar::-webkit-scrollbar {
  display: none;
}

/* Hide scrollbar for Firefox */
.no-scrollbar {
  scrollbar-width: none;
}

/* Hide scrollbar for IE & Edge */
.no-scrollbar {
  -ms-overflow-style: none;
}
.no-scrollbar::-ms-scrollbar {
  display: none;
}























// src/components/Footer/Footer.tsx
import { useState, useEffect } from "react";
import { motion } from "framer-motion";
import { useTheme } from "../../context/ThemeContext";
import {
  Instagram, Twitter, Youtube, Send, LucideFacebook,
  Mail, Phone, ArrowUp, MessageCircle, Shield, Globe
} from "lucide-react";
import GhanaFlagStripe from "../Ghana/GhanaFlagStripe";

const Footer = () => {
  const { theme } = useTheme();
  const isDark = theme === "dark";
  const currentYear = new Date().getFullYear();
  const [showScrollTop, setShowScrollTop] = useState(false);

  useEffect(() => {
    const toggle = () => setShowScrollTop(window.pageYOffset > 600);
    window.addEventListener("scroll", toggle);
    return () => window.removeEventListener("scroll", toggle);
  }, []);

  const scrollToTop = () => window.scrollTo({ top: 0, behavior: "smooth" });

  const socials = [
    { Icon: Instagram, href: "https://www.instagram.com/eyram_dela", label: "Instagram" },
    { Icon: Twitter, href: "https://x.com/eyramdela", label: "X" },
    { Icon: Youtube, href: "https://www.youtube.com/@marketgodcommunity", label: "YouTube" },
    { Icon: Send, href: "https://t.me/marketgodcommunity", label: "Telegram" },
    { Icon: LucideFacebook, href: "https://web.facebook.com/eyram.akpey", label: "Facebook" },
  ];

  const links = {
    company: [
      { label: "About Us", href: "/about" },
      { label: "Blog", href: "/blog" },
      { label: "Contact", href: "/contact" },
      { label: "Plans", href: "/plans" },
    ],
    resources: [
      { label: "Community", href: "https://t.me/marketgodcommunity" },
      { label: "Mentorship", href: "/plans" },
      { label: "VIP Signals", href: "/plans/#signals" },
      { label: "Free Content", href: "https://www.youtube.com/@marketgodcommunity" },
    ],
  };

  return (
    <>
      {/* Back to Top — Now Ultra Premium */}
      <motion.button
        initial={{ opacity: 0, scale: 0 }}
        animate={showScrollTop ? { opacity: 1, scale: 1 } : { opacity: 0, scale: 0 }}
        whileHover={{ scale: 1.2, rotate: 90 }}
        whileTap={{ scale: 0.9 }}
        onClick={scrollToTop}
        className="fixed bottom-8 right-8 z-50 p-4 rounded-full bg-mg-gold text-black shadow-2xl hover:shadow-gold-glow-lg transition-all duration-500 border-4 border-black dark:border-white"
        aria-label="Back to top"
      >
        <ArrowUp size={28} className="font-bold" />
      </motion.button>

      <footer className={`relative overflow-hidden pt-32 pb-16 ${isDark ? "bg-black" : "bg-mg-paper"}`}>
        {/* Subtle Logo Background */}
        <div className="absolute inset-0 opacity-5 pointer-events-none">
          <img src="/logo.png" alt="" className="w-full h-full object-cover object-center" />
        </div>

        {/* Golden Ambient Glow */}
        <div className="absolute inset-0 pointer-events-none opacity-30">
          <div className="absolute top-20 left-20 w-96 h-96 bg-mg-gold/20 rounded-full blur-3xl" />
          <div className="absolute bottom-20 right-20 w-96 h-96 bg-mg-gold/20 rounded-full blur-3xl" />
        </div>

        <GhanaFlagStripe />

        {/* 3D Slogan — Now Cleaner */}
        <div className="absolute inset-0 flex items-center justify-center pointer-events-none select-none">
          <motion.div
            initial={{ opacity: 0, rotateX: -60 }}
            whileInView={{ opacity: 0.08, rotateX: -45 }}
            viewport={{ once: true }}
            transition={{ duration: 1.5 }}
            className="text-8xl md:text-9xl lg:text-[12rem] font-black tracking-[0.3em] uppercase"
            style={{ color: "#D4AF37" }}
          >
            MARKETGOD ACADEMY
          </motion.div>
        </div>

        <div className="relative z-10 max-w-7xl mx-auto px-6">
          <div className="grid md:grid-cols-4 gap-12 lg:gap-16">

            {/* BRAND COLUMN */}
            <motion.div
              initial={{ opacity: 0, y: 50 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.8 }}
              className="flex flex-col gap-8"
            >
              <div className="flex items-center gap-4">
                <motion.div whileHover={{ rotate: 360 }} transition={{ duration: 0.8 }}>
                  <img src="/logo.png" alt="MarketGod" className="w-16 h-16 rounded-full border-2 border-mg-white shadow-2xl bg-black" />
                </motion.div>
                <div className={`${isDark ? "text-mg-white" : "text-mg-black"}`}>
                  <h3 className="text-3xl font-black text-g-gold">MarketGod</h3>
                  <p className="text-sm font-bold tracking-wider">ACADEMY</p>
                </div>
              </div>

              <p className={`text-sm leading-relaxed ${isDark ? "text-mg-white" : "text-mg-black"}`}>
                Empowering <span className="text-mg-gold font-bold">African traders</span> with elite mentorship, real-time signals, and proven strategies — from Accra to the world.
              </p>

              <div className="flex gap-4">
                {socials.map(({ Icon, href, label }, i) => (
                  <motion.a
                    key={i}
                    href={href}
                    target="_blank"
                    rel="noopener"
                    aria-label={label}
                    initial={{ opacity: 0, y: 20 }}
                    whileInView={{ opacity: 1, y: 0 }}
                    viewport={{ once: true }}
                    transition={{ delay: i * 0.1 }}
                    whileHover={{ scale: 1.3, rotate: 15 }}
                    className="p-3 rounded-full bg-mg-gold/10 border border-mg-gold/30 hover:bg-mg-gold hover:text-black transition-all duration-300"
                  >
                    <Icon size={20} />
                  </motion.a>
                ))}
              </div>
            </motion.div>

            {/* COMPANY */}
            <motion.div
              initial={{ opacity: 0, y: 50 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ delay: 0.2, duration: 0.8 }}
            >
              <h3 className={`text-lg font-bold mb-6  ${isDark ? "text-mg-white" : "text-mg-black"} tracking-wider`}>Company</h3>
              <ul className="space-y-4">
                {links.company.map((link, i) => (
                  <motion.li
                    key={i}
                    initial={{ opacity: 0, x: -20 }}
                    whileInView={{ opacity: 1, x: 0 }}
                    transition={{ delay: i * 0.1 }}
                  >
                        <a
                          href={link.href}
                         className={`
                          text-sm flex items-center gap-3 group transition-all duration-300
                          ${isDark ? "text-mg-white hover:text-mg-white/50" : "text-mg-black hover:text-mg-black/50"}
                        `}
                        >
                          <span
                            className={`
                              w-2 h-2 ${isDark ? "bg-mg-white" : "bg-mg-black"} rounded-full 
                              scale-0 group-hover:scale-100 
                              transition-transform duration-300 ease-out
                               ${isDark ? "text-mg-white hover:text-mg-white/50" : "text-mg-black hover:text-mg-black/50"}
                            `}
                          />
                          {link.label}
                        </a>
                  </motion.li>
                ))}
              </ul>
            </motion.div>

            {/* RESOURCES */}
            <motion.div
              initial={{ opacity: 0, y: 50 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ delay: 0.3, duration: 0.8 }}
            >
              <h3 className={`text-lg font-bold mb-6  ${isDark ? "text-mg-white" : "text-mg-black"} tracking-wider`}>Resources</h3>
              <ul className="space-y-4">
                {links.resources.map((link, i) => (
                  <motion.li
                    key={i}
                    initial={{ opacity: 0, x: -20 }}
                    whileInView={{ opacity: 1, x: 0 }}
                    transition={{ delay: i * 0.1 + 0.1 }}
                  >
                       <a
                        href={link.href}
                         className={`
                          text-sm flex items-center gap-3 group transition-all duration-300
                          ${isDark ? "text-mg-white hover:text-mg-white/50" : "text-mg-black hover:text-mg-black/50"}
                        `}
                        >
                        <span
                          className={`
                            w-2 h-2 ${isDark ? "bg-mg-white" : "bg-mg-black"} rounded-full 
                            scale-0 group-hover:scale-100 
                            transition-transform duration-300 ease-out
                          `}
                        />
                       {link.label}
                   </a>
                     </motion.li>
                ))}
              </ul>
            </motion.div>

            {/* COMMUNITY & CTA */}
            <motion.div
              initial={{ opacity: 0, y: 50 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ delay: 0.4, duration: 0.8 }}
              className="flex flex-col gap-8"
            >
              <div>
                <h3 className={`text-lg font-bold mb-4 ${isDark ? "text-mg-white" : "text-mg-black"}`}>Join 12,000+ Traders</h3>
                <p className={`text-sm mb-6 ${isDark ? "text-gray-400" : "text-gray-600"}`}>
                  Get live signals, free tools, and 24/7 support.
                </p>
              </div>

              <div className={`space-y-3 text-sm ${isDark ? "text-mg-white" : "text-mg-black"}`}>
                <a href="mailto:support@marketgod.academy" className="flex items-center gap-3 hover:text-mg-gold transition">
                  <Mail size={16} /> support@marketgod.academy
                </a>
                <a href="tel:+233557777777" className="flex items-center gap-3 hover:text-mg-gold transition">
                  <Phone size={16} /> +233 55 777 7777
                </a>
              </div>

              {/* MAIN CTA — Now PERFECT */}
              <motion.a
                href="https://t.me/delatrades"
                target="_blank"
                whileHover={{ scale: 1.05 }}
                whileTap={{ scale: 0.98 }}
                className={`
                  inline-flex items-center justify-center gap-3 px-8 py-5 rounded-full font-bold uppercase tracking-wider shadow-2xl
                  transition-all duration-500 hover:shadow-gold-glow-lg text-lg
                  bg-black text-white hover:bg-mg-gold hover:text-black
                  dark:bg-mg-gold dark:text-black dark:hover:bg-white dark:hover:text-black
                `}
              >
                <MessageCircle size={24} />
                Chat on Telegram Now
              </motion.a>
            </motion.div>
          </div>

          {/* FOOTER BOTTOM */}
          <motion.div
            initial={{ opacity: 0 }}
            whileInView={{ opacity: 1 }}
            viewport={{ once: true }}
            transition={{ delay: 0.6 }}
            className="mt-20 pt-10 border-t border-gray-800 dark:border-gray-200/20 text-center text-xs"
          >
            <p className="text-mg-gold font-bold mb-2">© {currentYear} MarketGod Academy. All rights reserved.</p>
            <p className={`italic mb-4 ${isDark ? "text-gray-500" : "text-gray-600"}`}>
              Trading involves substantial risk. Past performance is not indicative of future results.
            </p>
            <p className="text-mg-gold font-bold">Proudly crafted by Eugene Afriyie</p>
          </motion.div>
        </div>
      </footer>
    </>
  );
};

export default Footer;


